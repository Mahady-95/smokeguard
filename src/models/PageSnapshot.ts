export interface PageSnapshot {

    timestamp: number;

    url: string;

    title: string;

    rowCount: number;

    pageText: string;

    loadingVisible: boolean;

    noDataVisible: boolean;

    paginationVisible: boolean;

    searchValue: string;

}